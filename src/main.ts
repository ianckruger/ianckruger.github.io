import { createApp } from 'vue'
import App from './App.vue'

// Global styles (CSS variables, bootstrap, animations)
import './styles/variables.css'
import 'bootstrap/dist/css/bootstrap.min.css'
import 'animate.css'

createApp(App).mount('#app')
